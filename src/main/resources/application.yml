spring:
  application:
    name: forumhu
  profiles:
    active: dev

security:
  user:
    name: admin
    password: admin

springdoc:
  api-docs:
    path: /api/docs
  swagger-ui:
    path: /api/docs-ui

server:
  port: 8080
